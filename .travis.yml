sudo: false

language: node_js

node_js:
  - "4"
  - "6"

env:
  global:
    - NODE_ENV=test

before_script:
  - "mysql -u root -e 'create database actionhero_test'"

os:
  - linux

script: npm run test

services:
  - redis-server
  - mysql
